# Domain Docs

Before domain work, read the root `AGENTS.md`, `workflow-state.json` and its active task file, plus relevant `specs/` and `docs/adr/` entries.

Use `specs/product-contract.md` as the product contract and relevant files under `docs/` as supporting material. Follow `workflow/README.md` and `workflow/task-template.md` for task records. No root `CONTEXT.md` exists; add one only when domain modeling resolves durable shared terminology that is not already captured in the product contract.

Use established project terminology. Surface conflicts with existing ADRs and product contract rather than silently overwriting them. This is a single-project repository.
